server:
  port: 8020

spring:
  application:
    name: inventory
  datasource:
    url: jdbc:mysql://localhost:3301/inventoryDb
    username: root
    password: inventory
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
