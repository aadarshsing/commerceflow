
server:
  port: 8010

spring:
  application:
    name: catalog
  datasource:
    url: jdbc:mysql://localhost:3300/catalogDb
    username: root
    password: catalog
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

